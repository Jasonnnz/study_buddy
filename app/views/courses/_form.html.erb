<% if flash[:errors] %>
    <% flash[:errors].each do |error| %>
        <p class="error"><%= error %></p>
    <% end %>
<% end %>

<%= form_for @course do |f| %>
    <%= f.label :name %>
    <%= f.text_field :name %><br><br>
    <%= f.submit %>
<% end %>